{
  "name": "game-canvas-scaffold",
  "version": "1.0.0",
  "description": "HTML5 canvas Pong game with ephemeral users/scores + MySQL-backed style preferences",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/migrate.js && node server.js",
    "migrate": "node scripts/migrate.js",
    "test": "jest --forceExit --runInBand"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.js"]
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-session": "^1.18.0",
    "mysql2": "^3.11.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "prisma": "^5.19.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
