apiVersion: v1
kind: Service
metadata:
  name: pong-app
  labels:
    app: pong-app
spec:
  type: LoadBalancer
  selector:
    app: pong-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
